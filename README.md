# 🏨 Hotel Management System

A Python-based application to manage hotel room bookings, customer details, and billing.

## ✨ Features
- **View Rooms**: Check availability and pricing of rooms.
- **Book Room**: Assign rooms to customers and calculate costs.
- **Check-out**: Generate bills and free up rooms automatically.
- **Data Persistence**: Saves all data to `hotel_data.json`, so bookings are not lost when the program closes.
- **Input Validation**: Prevents crashes from invalid inputs.

## 🚀 How to Run
1. Ensure you have Python installed.
2. Run the script:
   ```bash
   python "Hotel _Management_System.py"
   ```
3. Follow the on-screen menu instructions.

## 📂 Project Structure
- `Hotel _Management_System.py`: The main source code.
- `hotel_data.json`: Stores room and customer data safely.

## 🛠️ Requirements
- Python 3.x
- No external libraries required (uses standard `json` and `os`).
